@model List<SGS.MultiTenancy.Core.Application.DTOs.Tenants.TenantDto>


@{
    ViewData["Title"] = "Tenants";
}

<div class="container-fluid px-4">
    <div class="d-flex justify-content-between align-items-center my-4">
        <h3 class="fw-bold m-0">Tenants</h3>
        <a asp-action="AddTenant" class="btn shadow-sm d-flex align-items-center gap-2">
            <i class="bi bi-plus-circle me-2"></i> Create Tenant
        </a>
    </div>

    <div class="card-body p-0">
        <table class="table text-center p-4">
            <thead class="table-header">
                <tr>
                    <th>Name</th>
                    <th>Business Name</th>
                    <th>Email</th>
                    <th>Phone</th>
                    <th>Status</th>
                    <th>Address</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                @if (Model != null && Model.Any())
                {
                    foreach (var t in Model)
                    {
                        <tr>
                            <td>@t.Name</td>
                            <td>@t.BussinessName</td>
                            <td>@t.Email</td>
                            <td>@t.PhoneNumber</td>
                            <td>@t.Status</td>
                            <td>
                                <button type="button"
                                        class="btn btn-sm  view-address-btn"
                                        data-bs-toggle="modal"
                                        data-bs-target="#addressModal"
                                        data-line1="@t.AddressLine"
                                        data-city="@t.City"
                                        data-state="@t.State"
                                        data-country="@t.Country"
                                        data-postal="@t.PostalCode">
                                    <i class="bi bi-eye"></i>
                                </button>
                            </td>
                            <td class="text-center">
                                <button class="btn btn-sm btn-outline-custom"
                                        data-bs-toggle="modal"
                                        data-bs-target="#editTenantModal"
                                        data-id="@t.ID"
                                        title="Edit" style="border:none;">
                                    <i class="bi bi-pencil-square  fs-5"></i>
                                </button>

                                <button class="btn btn-sm btn-outline-custom"
                                        data-bs-toggle="modal"
                                        data-bs-target="#deleteTenantModal"
                                        data-tenant-id="@t.ID"
                                        data-tenant-name="@t.Name" style="border:none;">
                                    <i class="bi bi-trash   fs-5"></i>
                                </button>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="7" class="text-center text-muted">No tenants found.</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

@* model *@

<div class="modal fade" id="addressModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog modal-dialog-centered modal-dialog-scrollable">
        <div class="modal-content shadow-sm">

            <div class="modal-header text-white"
                 style="background-color:#00305f;">
                <h6 class="modal-title mb-0">
                    <i class="bi bi-geo-alt-fill me-1"></i>
                    Tenant Address
                </h6>
                <button type="button" class="btn-close btn-close-white"
                        data-bs-dismiss="modal"></button>
            </div>

            <div class="modal-body py-2">
                <dl class="row mb-0 small">
                    <dt class="col-5 text-muted">Address</dt>
                    <dd class="col-7" id="modalLine1"></dd>

                    <dt class="col-5 text-muted">City</dt>
                    <dd class="col-7" id="modalCity"></dd>

                    <dt class="col-5 text-muted">State</dt>
                    <dd class="col-7" id="modalState"></dd>

                    <dt class="col-5 text-muted">Country</dt>
                    <dd class="col-7" id="modalCountry"></dd>

                    <dt class="col-5 text-muted">Postal</dt>
                    <dd class="col-7" id="modalPostal"></dd>
                </dl>
            </div>
            <div class="modal-footer ">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>

<!-- Include Modal Partials -->
@await Html.PartialAsync("_EditTenantModal")
@await Html.PartialAsync("_DeleteTenantModal")


