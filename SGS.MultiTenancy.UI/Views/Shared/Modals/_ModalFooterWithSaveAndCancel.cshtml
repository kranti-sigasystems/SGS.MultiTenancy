@model bool
@* true = modal, false = page  *@

<div class="d-flex justify-content-end mt-4">
    <button type="submit" class="btn btn-primary">
        Save
    </button>

    @if (Model)
    {
        <button type="button"
                class="btn btn-gray-200 ms-2"
                data-bs-dismiss="modal">
            Cancel
        </button>
    }
    else
    {
        <a asp-action="Index"
           class="btn btn-gray-200 ms-2">
            Cancel
        </a>
    }
</div>
